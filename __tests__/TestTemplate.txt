import React from 'react';
import "babel-polyfill";
import YOUR TEST COMPONENT from WHEREVER';
import { render, fireEvent, waitFor, screen } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';

import chrome from "sinon-chrome";
global.chrome = chrome;

/* Uncomment this in your file
test('ENTER YOUR TEST MESSAGE', () => {
  render(<YOUR COMPONENT />)

  fireEvent.click(screen.getByText('Blah'))

  await waitFor(() => screen.getByRole('blah'))

  expect(screen.getByRole('blah')).toHaveTextContent('blah')
  expect(screen.getByRole('blah')).toHaveAttribute('blah')
});

*/
